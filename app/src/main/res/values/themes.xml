<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ChatApp" parent="Theme.AppCompat.DayNight">
        <item name="android:textColorPrimary">#000000</item>
    </style>

    <style name="Theme.ChatApp" parent="Base.Theme.ChatApp" />

    <style name="Theme.ChatApp.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/aloha</item>
        <item name="postSplashScreenTheme">@style/Theme.ChatApp</item>
    </style>
</resources>